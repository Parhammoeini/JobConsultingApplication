services:
  db:
    image: pgvector/pgvector:pg16
    container_name: job_platform_db
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB=job_db
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=password